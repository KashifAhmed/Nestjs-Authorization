export class AuthorizationService {
    constructor(){}

    async registerUser(userid: string, password: string){
        // Before save of user must encrypt the password with encrypt strategy
    }
}